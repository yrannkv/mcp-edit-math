# Указываем, что используем Docker
runtime: container

build:
  dockerfile: Dockerfile
  dockerBuildPath: .

# Важно: указываем тип http, чтобы Smithery знал, как проверять здоровье сервера
startCommand:
  type: http