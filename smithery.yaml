# Инструкция сборки: Устанавливаем текущую папку как пакет
buildCommand: pip install .

# Инструкция запуска: Вызываем команду, которая создалась при установке
startCommand:
  type: stdio
  command: mcp-edit-math